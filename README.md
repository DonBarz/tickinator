Ignores selected block entity ticks by injecting into the server level tick.

Which blocks to ignore can be decided via the fern configuration (see .minecraft/config/tickinator.json5)
Now compatible with observable
