Ignores selected block entity ticks by injecting into the server level tick.

Which blocks to ignore can be decided via a datapack.
Add the blocks to "resources/data/tickinator/tags/blocks/include.json" and "resources/data/tickinator/tags/blocks/exclude.json" tags.
Included blocks will not tick, if added to both, the inclusion will have priority.
